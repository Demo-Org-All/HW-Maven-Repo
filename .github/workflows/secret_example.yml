name: Github Secret Example

on:
  push:
    branches: [ master ]
  
jobs:
  Docker_Login_Check:
    runs-on: self-hosted
    steps: 
      - run: |
          docker login -u vniranjan1972 -p ${{ secrets.DOCKERHUB_PASSWORD }}
          docker ps -a
